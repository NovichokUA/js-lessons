{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Запустить сценарий: test",
            "request": "launch",
            "command": "npm run test",
            "cwd": "${workspaceFolder}"
        }
    ]
}